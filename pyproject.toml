[project]
name = "odoo-cli-official"
dynamic = ["version"]
description = "CLI tool to manage and develop local Odoo instances"
requires-python = ">=3.11"
readme = "README.md"
license = "MIT"
authors = [
    { name = "Frederic van der Essen" },
]
keywords = ["odoo", "cli", "development", "postgresql"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development",
]
# Runtime is stdlib plus the vendored click under odoo_cli/_vendor.
dependencies = []

[project.urls]
Homepage = "https://github.com/fvdsn/odoo-cli"
Repository = "https://github.com/fvdsn/odoo-cli"
Issues = "https://github.com/fvdsn/odoo-cli/issues"

[project.scripts]
odoo = "odoo_cli.cli.main:main"

[tool.hatch.version]
path = "odoo_cli/__init__.py"

[tool.ruff]
line-length = 100
target-version = "py311"
extend-exclude = ["odoo_cli/_vendor"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.hatch.build.targets.wheel]
packages = ["odoo_cli"]

[tool.hatch.build.targets.sdist]
exclude = [".claude", "/dist"]

[tool.uv]
dev-dependencies = [
    "ruff>=0.15.14",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
